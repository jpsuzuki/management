<!DOCTYPE html>
<html lang="ja">
<html>
  <head>
    <meta charset="utf-8">
    <title><%= yield(:title) %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/r29/html5.min.js">
      </script>
    <![endif]-->
  </head>

  <body>
    <header class="navbar sticky-top bg-black">
      <div class="container d-flex justify-content-between">
          <% if logged_in? %>
            <% if current_user.admin? %>
              <%= link_to "山水", users_path, class: "h1 text-light"  %>
            <% else %>
              <%= link_to "山水", root_path, class: "h1 text-light"  %>
            <%end%>
            <%= link_to "ログアウト", logout_path, 
            method: :delete, class: "btn btn-outline-danger" %>
          <% else %>
            <div class="h1 text-light">
              山水
            </div>
          <% end %>
        </div>
      </div>
    </header>
    <main>
      <% flash.each do |message_type, message| %>
        <div class="container my-4 w-80 alert alert-<%= message_type %>"><%= message %></div>
      <% end %>
      <%= yield %>
      <%= debug(params) if Rails.env.development? %>
    </main>
  </body>
</html>
